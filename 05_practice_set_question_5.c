#include<stdio.h>

int main(){
int a=3;
printf("%d %d %d",a,a++,a++);                    //when we pass argument to printf it goes right to left ...
    return 0;                                    //depends on compiler sometimes..
    //interview question.
    // compiler behaviour.
}